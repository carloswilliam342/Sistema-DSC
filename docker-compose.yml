services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      DB_USER: ${DB_USER:-root}
      DB_PASSWORD: ${DB_PASSWORD:-root}
      DB_NAME: ${DB_NAME:-dsc_ifma}
      GEMINI_API_KEY: ${GEMINI_API_KEY}
      SESSION_SECRET: ${SESSION_SECRET}
      BASE_PATH: ${BASE_PATH:-}
    volumes:
      - ./uploads:/app/uploads
    depends_on:
      migrate:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000/health', res => process.exit(res.statusCode === 200 ? 0 : 1)).on('error', () => process.exit(1))"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: unless-stopped

  migrate:
    build: .
    command: ["npx", "sequelize-cli", "db:migrate"]
    environment:
      # Usuário com privilégio de DDL, separado do usuário "leve" do app.
      # Sem DB_MIGRATE_USER/PASSWORD definidos, cai no mesmo usuário do app
      # (comportamento anterior, não quebra ambientes que ainda não migraram).
      DB_USER: ${DB_MIGRATE_USER:-${DB_USER:-root}}
      DB_PASSWORD: ${DB_MIGRATE_PASSWORD:-${DB_PASSWORD:-root}}
      DB_NAME: ${DB_NAME:-dsc_ifma}
    restart: "no"
